# Counter-Program
This is a counter program using PyQt5

# Needed dependencies
This project needs **PyQt5** to run so use **pip install PyQt5** in your terminal

# Run
To run this app you need to run **python main.py**
